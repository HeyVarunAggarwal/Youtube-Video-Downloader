This is User Authentication System Using Flask
This project is the initial release and will be improved in future.

Pre-requestics:
1. pip install flask
2. pip install flask_sqlalchemy
3. pip install flask_login